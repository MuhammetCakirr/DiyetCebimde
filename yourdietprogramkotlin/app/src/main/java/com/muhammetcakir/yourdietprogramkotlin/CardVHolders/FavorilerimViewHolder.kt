package com.muhammetcakir.yourdietprogramkotlin.CardVHolders
/*
import androidx.recyclerview.widget.RecyclerView
import com.muhammetcakir.yourdietprogramkotlin.ClickListener.BookClickListener
import com.muhammetcakir.yourdietprogramkotlin.Models.Book
import com.muhammetcakir.yourdietprogramkotlin.databinding.CardCellBinding

class FavorilerimViewHolderViewHolder(
    private val cardCellBinding: CardCellBinding,
    private val clickListener: BookClickListener
) : RecyclerView.ViewHolder(cardCellBinding.root)
{
    fun bindBook(book: Book)
    {
        cardCellBinding.cover.setImageResource(book.cover)
        cardCellBinding.title.text = book.title
        cardCellBinding.author.text = book.author

        cardCellBinding.cardView.setOnClickListener{
            clickListener.onClick(book)
        }
    }
}*/